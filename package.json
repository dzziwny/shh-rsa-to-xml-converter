{
  "name": "ssh",
  "private": true,
  "version": "0.0.1",
  "description": "ssh",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "build:prod": "stencil build --prod",
    "start": "stencil build --dev --watch --serve",
    "start:prod": "stencil build --prod --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "@ionic/core": "^5.0.0",
    "@stencil/core": "^1.8.8",
    "firebase-tools": "^7.15.1",
    "workbox-build": "4.3.1"
  }
}
